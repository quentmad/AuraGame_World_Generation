package aura_game.app.GenerationMapS2;

/**
 * Cette classe contient des méthodes de test pour le générateur de carte.
 */
public class TestGenerateurCarte {


    /**
     * Méthode de test pour générer et afficher une carte avec des poids spécifiques.
     *
     * @param poids       Les poids des différents types de terrain.
     * @param randomSeed  La graine pour la génération aléatoire.
     */
    /*
    public static void testGenererCarte(int[] poids, long randomSeed) {
        GenerateurCarte generateurCarte = new GenerateurCarte();
        generateurCarte.setPoids(poids);
        generateurCarte.setGraineAleatoire(randomSeed);

        System.out.println("Test de génération de carte avec les poids : " + Arrays.toString(poids) + ", graine aléatoire : " + randomSeed);
        generateurCarte.afficherCarte();
        System.out.println("-----");
    }*/

    /**
     * Méthode de test pour simuler l'émergence de la carte avec des poids spécifiques.
     *
     * @param poids       Les poids des différents types de terrain.
     * @param randomSeed  La graine pour la génération aléatoire.
     */
    /*
    public static void testEmergerCarte(int[] poids, long randomSeed) {
        GenerateurCarte generateurCarte = new GenerateurCarte();
        generateurCarte.setPoids(poids);
        generateurCarte.setGraineAleatoire(randomSeed);

        System.out.println("Test d'émergence de carte avec les poids : " + Arrays.toString(poids) + ", graine aléatoire : " + randomSeed);
        generateurCarte.emergerCarte();
        System.out.println("-----");
    }*/
}
